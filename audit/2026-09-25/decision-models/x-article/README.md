# X article: "Three AI judges, four rounds, and the one change Smriti kept"

`x-article.md` is the text, with image markers in order: `0-cover.png`
(5:2 header) and `1-` to `5-` (inline, 16:9). Graphics are rendered from
`graphics.html` with `node shoot.js` (fonts in `./fonts`, see
`../explainer-video/README.md`). Every number comes from `../NOTES.md`.
